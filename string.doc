fun main(){
    println(loveFun(3,6))
     println(repeatStr(4,"Numerous possibilities"))
}



fun loveFun(flowerA: Int, flowerB: Int): Boolean {
  // moment of truth
    if(flowerA%2==0 && flowerB%2!=0){
        return true            //boolean is to be in small
    }
    else if(flowerA%2!=0 && flowerB%2==0){
         return  true
    }
    
    else{
        
    }
    return false//return usually outside brackets.
}
fun repeatStr(r: Int, str: String) : String {
    var repeat = str.repeat(r)
    return repeat   //we use a repeat function to multiply a string
}
